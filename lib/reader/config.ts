/**
 * Maximum character count for the book description preview shown in catalog cards.
 * Truncation is applied to extracted plain text, not raw Tiptap JSON.
 * Adjust this value during visual polish — it propagates everywhere automatically.
 */
export const BOOK_CARD_DESCRIPTION_LIMIT = 320;
